package screenShots;

import java.awt.AWTException;
import java.awt.Color;
import java.awt.Rectangle;
import java.awt.Robot;
import java.awt.Toolkit;
import java.awt.image.BufferedImage;

import file.WireID;

public class ScreenDetection {

	public static void main(String[] args) throws AWTException {
	        // 캡쳐할 화면 영역 설정
	        Rectangle screenRect = new Rectangle(Toolkit.getDefaultToolkit().getScreenSize());

	        // 화면 캡쳐
	        BufferedImage capture = new Robot().createScreenCapture(screenRect);

	        int x1 = 765; int x2 = 710; int x3 = 665; int x4 = 610; int y;
	        
	        if (WireID.getWireID().contains("sit")) { y = 700; }
	        else if (WireID.getWireID().contains("km")) { y = 690; }
	        
	        // 캡쳐된 이미지에서 특정 좌표의 RGB값 가져오기
	        Color color1 = new Color(capture.getRGB(x1, y));
	        Color color2 = new Color(capture.getRGB(x2, y));
	        Color color3 = new Color(capture.getRGB(x3, y));
	        Color color4 = new Color(capture.getRGB(x4, y));
	        
	        if (color1)

	        // RGB값 출력
	        System.out.println("RGB value of (" + x + ", " + y + "):");
	        System.out.println("Red: " + color.getRed());
	        System.out.println("Green: " + color.getGreen());
	        System.out.println("Blue: " + color.getBlue());
	    }
}




