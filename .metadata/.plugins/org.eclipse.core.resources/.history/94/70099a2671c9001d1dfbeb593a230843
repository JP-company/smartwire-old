package test;

import java.io.BufferedWriter;
import java.io.FileWriter;
import java.io.IOException;
import java.util.LinkedList;
import java.util.ListIterator;
import java.util.Queue;

import timeFunctions.CurrentTime;

public class Test {

	public static void main(String[] args) throws IOException {
		Queue<String> dateData = new LinkedList<String>();
		
		for (int i = 0; i < 10; i++)
			dateData.offer(i + "");
		
		String str = "";
		String str2 = "";
		LinkedList<String> tmp = (LinkedList<String>)dateData;
		ListIterator<String> it = tmp.listIterator();
		
//		while(it.hasNext()){
//			str = str +it.next() + " ";
//		}
		
		String file = String.format("C:/spmEzCut/LogMessage/%s.log", CurrentTime.getTime("date"));
		
		while(it.hasNext()){
			FileWriter fw = new FileWriter(file);
            BufferedWriter writer = new BufferedWriter(fw);
            writer.write(it.next());
            
            System.out.println("d");
		}
		
//		while(it.hasNext()){
//			str2 = str2 +it.next() + " ";
//		}
		
		System.out.println(str.trim());
		System.out.println(str2.trim());

	}

}
